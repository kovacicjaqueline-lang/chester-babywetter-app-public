{
  "name": "chester-babywetter-app-public",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "test": "node --test",
    "test:workflow": "node --test test/workflow-ci.test.js",
    "test:repository-guard": "node --test test/repository-guard.test.js",
    "check:node-compat": "node scripts/check-node-compat.mjs",
    "guard:pre-push": "node scripts/repository-guard.mjs check",
    "workflow:finalize": "node scripts/repository-guard.mjs lock",
    "workflow:repair": "node scripts/repository-guard.mjs unlock --repair",
    "test:browser": "playwright test",
    "verify:deploy": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "wrangler": "4.125.0"
  }
}
